name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

jobs:
  biome-format-lint-check:
    name: Check
    uses: SnakeDoc/wontonium/.github/workflows/biome.yaml@ae94f53fdecb3b3e9dc7c0f051456218938f0183 # main
