name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

jobs:
  biome-format-lint-check:
    name: Check
    uses: SnakeDoc/wontonium/.github/workflows/biome.yaml@322e66c2ad134ab2ca1e77b432ecc4fb7fd6fbde # main
